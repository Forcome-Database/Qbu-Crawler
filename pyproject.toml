[project]
name = "basspro-scraper"
version = "0.1.0"
description = "Bass Pro Shops 产品爬虫服务"
requires-python = ">=3.10"
dependencies = [
    "drissionpage",
    "fastapi",
    "fastmcp>=2.0.0",
    "minio",
    "python-dotenv",
    "requests",
    "uvicorn[standard]",
]

[tool.uv]
package = false
